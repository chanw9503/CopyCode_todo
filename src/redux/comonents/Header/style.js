import styled from 'styled-components';

// header 박스
const StlyedHeader = styled.div`
  background-color: #e8ffee;
  padding: 20px;
  font-size: larger;
  font-weight: 700;
  display: flex;
  justify-content: space-between;
`;

//header에 쓸 문자
const StyledP = styled.p`
  margin: 0;
`;

export { StlyedHeader, StyledP };
